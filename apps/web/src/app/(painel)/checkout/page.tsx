import { CheckoutLayout } from "@/components/layout/checkout-layout";

export default function Page() {
	return (
		<CheckoutLayout>
			{/* Adicione aqui os componentes/etapas específicas do checkout, como formulários de endereço/pagamento, etc. */}
			<div />
		</CheckoutLayout>
	);
}
